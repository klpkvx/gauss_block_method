#ifndef ERRORS_H
#define ERRORS_H

enum class ERRORS
{
	SUCCESS = 0,
	ARGS = -1,
	CANNOT_OPEN = -2,
	CANNOT_ALLOCATE = -3,
};
enum class is_calc
{
	calculcated = 1,
	not_calculated = 0,
};

#endif // ERRORS_H